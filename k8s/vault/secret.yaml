# WARNING: This is a dev-only secret with a placeholder token.
# In production, use cloud KMS (e.g., AWS KMS, GCP Cloud KMS) or Sealed Secrets.
# NEVER commit real secrets to version control.
apiVersion: v1
kind: Secret
metadata:
  name: vault-dev-token
  namespace: orbitpay
  labels:
    app.kubernetes.io/name: vault
    app.kubernetes.io/part-of: orbitpay
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # Base64 of "dev-root-token-orbitpay" — placeholder only, NOT a real secret
  VAULT_DEV_ROOT_TOKEN_ID: ZGV2LXJvb3QtdG9rZW4tb3JiaXRwYXk=
